{
  "name": "mcpwarden",
  "version": "0.1.0",
  "description": "A policy-checkable MCP workflow runtime with safe tool discovery, approval planning, and a local service API.",
  "private": false,
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.33.4",
  "bin": {
    "mcpw": "./dist/cli.js"
  },
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "templates",
    "examples",
    "docs",
    "LICENSE",
    "CHANGELOG.md",
    "SECURITY.md",
    "README.md",
    "package.json"
  ],
  "engines": {
    "node": ">=22"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Gravitied/MCPWarden.git"
  },
  "homepage": "https://github.com/Gravitied/MCPWarden#readme",
  "bugs": {
    "url": "https://github.com/Gravitied/MCPWarden/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "mcp",
    "workflow",
    "policy",
    "agent-runtime",
    "tool-safety"
  ],
  "scripts": {
    "clean": "node scripts/clean-dist.mjs",
    "build": "pnpm clean && tsc -p tsconfig.build.json",
    "check": "pnpm build && pnpm lint",
    "lint": "eslint .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:security": "vitest run tests/security",
    "test:effectiveness": "vitest run tests/eval",
    "test:e2e": "vitest run tests/e2e",
    "test:package": "vitest run tests/e2e/package-smoke.test.ts",
    "release:check": "pnpm check && pnpm test && pnpm test:package && pnpm audit --audit-level low",
    "bench": "vitest bench tests/bench",
    "cli": "tsx src/cli.ts",
    "prepack": "pnpm build"
  },
  "dependencies": {
    "@commander-js/extra-typings": "^14.0.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "commander": "^14.0.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.6.2",
    "@vitest/coverage-v8": "^4.1.5",
    "eslint": "^10.3.0",
    "fast-check": "^4.7.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.2",
    "vitest": "^4.1.5"
  }
}
